[
    {
        "name": "libhpdf-dev",
        "commands": [
            "sudo apt-get install -y libhpdf-dev",
            "sudo ln -sf /usr/include/hpdf* /usr/x86_64-w64-mingw32/include/"
        ]
    },
    {
        "name": "libepub-dev",
        "commands": [
            "sudo apt-get install -y libepub-dev",
            "sudo ln -sf /usr/include/epub* /usr/x86_64-w64-mingw32/include/"
        ]
    },
    {
        "name": "libcli11-dev",
        "commands": [
            "sudo apt-get install -y libcli11-dev",
            "sudo ln -sf /usr/include/CLI /usr/x86_64-w64-mingw32/include/CLI"
        ]
    },
    {
        "name": "catch2",
        "commands": [
            "sudo apt-get install -y catch2"
        ]
    }
]