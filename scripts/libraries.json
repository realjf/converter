[
    {
        "name": "libhpdf-dev",
        "commands": [
            "sudo apt-get install -y libhpdf-dev",
            "sudo ln -sf /usr/include/hpdf* /usr/x86_64-w64-mingw32/include/"
        ]
    },
    {
        "name": "libepub-dev",
        "commands": [
            "sudo apt-get install -y libepub-dev",
            "sudo ln -sf /usr/include/epub* /usr/x86_64-w64-mingw32/include/"
        ]
    },
    {
        "name": "libcli11-dev",
        "commands": [
            "sudo apt-get install -y libcli11-dev",
            "sudo ln -sf /usr/include/CLI /usr/x86_64-w64-mingw32/include/CLI"
        ]
    },
    {
        "name": "catch2",
        "commands": [
            "sudo apt-get install -y catch2",
            "sudo ln -sf /usr/include/catch2 /usr/x86_64-w64-mingw32/include/catch2"
        ]
    },
    {
        "name": "libspdlog-dev",
        "commands": [
            "sudo apt-get install -y libspdlog-dev",
            "sudo ln -sf /usr/include/spdlog /usr/x86_64-w64-mingw32/include/spdlog"
        ]
    },
    {
        "name": "libfmt-dev",
        "commands": [
            "sudo apt-get install -y libfmt-dev",
            "sudo ln -sf /usr/include/fmt /usr/x86_64-w64-mingw32/include/fmt"
        ]
    },
    {
        "name": "gcc-11 g++-11 gcc-mingw-w64 g++-mingw-w64",
        "commands": [
            "sudo apt-get install -y gcc-11 g++-11 gcc-mingw-w64* g++-mingw-w64*"
        ]
    }
]